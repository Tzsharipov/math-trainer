# 📋 ИНСТРУКЦИЯ: Настройка Google Forms для приёма заявок

## ✅ ШАГ 1: Создание Google Form

1. Перейди на https://forms.google.com
2. Нажми "+ Пустая форма"
3. Заголовок: **"Заказ доступа к Математическому тренажёру"**
4. Описание: **"Заполните форму для получения реквизитов оплаты"**

---

## 📝 ШАГ 2: Добавление полей

### Поле 1: Имя
- Тип: **Текст (короткий ответ)**
- Вопрос: **"Ваше имя"**
- ✅ Обязательный вопрос

### Поле 2: Email
- Тип: **Текст (короткий ответ)**
- Вопрос: **"Ваш email"**
- Проверка ответов: **Email**
- ✅ Обязательный вопрос

### Поле 3: Телефон (опционально)
- Тип: **Текст (короткий ответ)**
- Вопрос: **"Телефон для связи (по желанию)"**
- ❌ НЕ обязательный

### Поле 4: Имя ребёнка
- Тип: **Текст (короткий ответ)**
- Вопрос: **"Имя ребёнка (для персонализации)"**
- Подсказка: "Например: Машенька, Саша, Даша"
- ❌ НЕ обязательный

### Поле 5: Класс обучения
- Тип: **Один из списка**
- Вопрос: **"В каком классе учится ребёнок?"**
- Варианты:
  - 2 класс
  - 3 класс
  - 4 класс
  - 5 класс
  - Другое
- ❌ НЕ обязательный

---

## 📧 ШАГ 3: Настройка автоответчика

1. Открой форму → вкладка **"Ответы"**
2. Нажми три точки (⋮) → **"Получать уведомления по электронной почте"**
3. Включи переключатель

### Настройка сообщения для пользователя:

1. **Настройки формы** (⚙️ справа вверху)
2. Вкладка **"Презентация"**
3. В поле **"Сообщение с подтверждением"** вставь:

```
Спасибо за заявку! 🎓

Реквизиты для оплаты отправлены на ваш email.
Проверьте папку "Входящие" (или "Спам").

Если письма нет — напишите нам:
📧 tzsharipov@gmail.com
💬 Telegram: @SharipovT
```

---

## 📬 ШАГ 4: Автоматическая отправка email (через Google Apps Script)

### Вариант А: Простой (уведомление на твой email)

1. Форма → **Ответы** → три точки (⋮)
2. **"Получать уведомления по электронной почте"**
3. Теперь при каждой заявке тебе придёт уведомление
4. Ты вручную отправляешь клиенту письмо с реквизитами

**Время обработки:** 2-5 минут на заявку

---

### Вариант Б: Автоматический (через Google Apps Script)

⚠️ **Требует 10-15 минут настройки, НО потом всё работает автоматически!**

#### 1. Открой редактор скриптов:
- Форма → три точки (⋮) → **"Редактор скриптов"**

#### 2. Вставь этот код:

```javascript
function sendEmailOnFormSubmit(e) {
  // Получаем ответы из формы
  const responses = e.response.getItemResponses();
  
  let name = '';
  let email = '';
  let childName = '';
  
  // Извлекаем данные
  responses.forEach(response => {
    const question = response.getItem().getTitle();
    const answer = response.getResponse();
    
    if (question.includes('имя') && question.includes('Ваше')) {
      name = answer;
    } else if (question.includes('email')) {
      email = answer;
    } else if (question.includes('ребёнка')) {
      childName = answer;
    }
  });
  
  // Формируем письмо
  const subject = '💳 Реквизиты для оплаты — Математический тренажёр';
  
  const body = `
Здравствуйте, ${name}!

Спасибо за интерес к Математическому тренажёру! 🎓

Для получения доступа переведите 990₽ одним из способов:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📱 ПО НОМЕРУ ТЕЛЕФОНА (СБП — РЕКОМЕНДУЕМ):
+7 925 090 72 41
(Тахир Жураевич Ш.)

💳 НА КАРТУ МИР:
2202 2056 3073 0999
(Шарипов Тахир Жураевич)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📝 ПОСЛЕ ОПЛАТЫ:

Ответьте на это письмо или напишите:
📧 Email: tzsharipov@gmail.com
💬 Telegram: @SharipovT

Укажите что оплатили — вышлю логин и пароль.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Доступ открывается в течение 1-2 часов
✅ Работает навсегда, без подписок
${childName ? `✅ Персонализация для ${childName}` : ''}

С уважением,
Тахир
Математический тренажёр
  `.trim();
  
  // Отправляем письмо
  MailApp.sendEmail(email, subject, body);
  
  // Уведомляем себя
  MailApp.sendEmail(
    'tzsharipov@gmail.com',
    '🔔 Новая заявка на тренажёр',
    `Новая заявка от ${name} (${email})\n\nИмя ребёнка: ${childName || 'не указано'}`
  );
}
```

#### 3. Настрой триггер:

1. В редакторе скриптов → **Триггеры** (⏰ слева)
2. **"Добавить триггер"**
3. Настройки:
   - Функция: **sendEmailOnFormSubmit**
   - Источник события: **Из формы**
   - Тип события: **При отправке формы**
4. Сохрани

#### 4. Дай разрешения:

- При первом запуске Google попросит разрешения
- Нажми **"Разрешить"**
- Выбери свой аккаунт
- **"Разрешить"** доступ к Gmail

**Готово!** Теперь при каждой заявке клиент автоматически получит письмо с реквизитами!

---

## 🔗 ШАГ 5: Получение ссылки на форму

1. Форма → **"Отправить"** (справа вверху)
2. Вкладка **"Ссылка"** (🔗)
3. **"Укоротить URL"** (чекбокс)
4. **Скопируй ссылку**

Пример: `https://forms.gle/abc123xyz`

---

## 🔄 ШАГ 6: Интеграция в лендинг

### Замени в файле `landing.html`:

Найди функцию `openOrderForm()` и замени на:

```javascript
function openOrderForm() {
  // Вставь свою ссылку на Google Form
  window.open('https://forms.gle/ВАША_ССЫЛКА_СЮДА', '_blank');
}
```

**Или используй iframe (форма откроется на той же странице):**

```javascript
function openOrderForm() {
  const modal = document.createElement('div');
  modal.style.cssText = `
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.8);
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;
  `;
  
  modal.innerHTML = `
    <div style="background: white; border-radius: 20px; max-width: 600px; width: 100%; max-height: 90vh; overflow: auto; position: relative; padding: 20px;">
      <button onclick="this.closest('div').parentElement.remove()" style="position: absolute; top: 10px; right: 10px; font-size: 24px; border: none; background: none; cursor: pointer;">✕</button>
      <iframe src="https://forms.gle/ВАШ А_ССЫЛКА_СЮДА" width="100%" height="600" frameborder="0"></iframe>
    </div>
  `;
  
  document.body.appendChild(modal);
}
```

---

## 📊 ШАГ 7: Просмотр заявок

Все заявки собираются:

1. **В Google Forms:**
   - Форма → **Ответы**
   - Можно смотреть по одной или списком

2. **В Google Sheets (таблица):**
   - Форма → Ответы → **"Создать таблицу"** (значок таблицы)
   - Все заявки в удобной таблице
   - Автоматически обновляется при новых ответах

---

## ✅ ИТОГО: Что получаем

1. Клиент жмёт **"Купить"** → открывается Google Form
2. Заполняет форму → жмёт **"Отправить"**
3. **Автоматически** получает письмо с реквизитами
4. **Ты получаешь** уведомление о новой заявке
5. Клиент переводит деньги
6. Клиент пишет тебе → ты даёшь доступ

**Время обработки: 5-10 минут на заявку**

---

## 🚀 Альтернатива: Tally.so (проще Google Forms)

Если Google Forms покажется сложным, попробуй **Tally.so**:

- Красивее интерфейс
- Проще настройка автоответов
- Бесплатно до 100 ответов/месяц
- https://tally.so

---

## 💡 Следующий шаг

После того как настроишь форму:

1. Скопируй ссылку на форму
2. Вставь в `landing.html` (в функцию `openOrderForm()`)
3. Залей обновлённый файл на Netlify
4. **Готово к продажам!** 🎉

---

Нужна помощь с настройкой? Пиши! 🚀
